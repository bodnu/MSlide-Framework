# Archive

Cleanup staging area for files excluded from the public MSlide³ starter.

No duplicate files, `.DS_Store`, `sftp-config`, or release noise were present in `MSlide3-Framework/` during the initial audit. Future cleanup should move files here before deletion.
